/**
 * Interface for the response object returned by the create function.
 * @param {string} text - The text property of the response object.
 */
export default interface CreateResponse {
  text: string;
}
